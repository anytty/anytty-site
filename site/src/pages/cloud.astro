---
import CloudLandingPage from '@/components/cloud-landing-page.astro'
import BaseLayout from '@/layouts/BaseLayout.astro'

const description = 'Reach persistent terminals from the AnyTTY TUI or mobile app with managed endpoint discovery, P2P negotiation, and encrypted Relay fallback.'
const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'AnyTTY Cloud',
  applicationCategory: 'DeveloperApplication',
  operatingSystem: 'Linux, macOS, Windows, Android, iOS',
  description,
  offers: [
    { '@type': 'Offer', name: 'Cloud Free', price: '0', priceCurrency: 'USD' },
    { '@type': 'Offer', name: 'Personal Pro monthly', price: '9.90', priceCurrency: 'USD' },
    { '@type': 'Offer', name: 'Power User monthly', price: '19.90', priceCurrency: 'USD' },
  ],
}
---

<BaseLayout locale="en" title="Cloud" description={description} pathname="/cloud/">
  <script is:inline slot="head" type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  <CloudLandingPage locale="en" />
</BaseLayout>
