---
import {
  ArrowDownToLine,
  ArrowRight,
  Check,
  CheckCircle2,
  Clock3,
  Cloud,
  Eye,
  FileStack,
  Files,
  History,
  Laptop,
  LockKeyhole,
  Minus,
  MonitorSmartphone,
  PanelsTopLeft,
  Radio,
  ShieldCheck,
  Smartphone,
  TerminalSquare,
} from 'lucide-react'

import { InstallTabs } from '@/components/install-tabs'
import { Badge } from '@/components/ui/badge'
import { buttonVariants } from '@/components/ui/button'
import { localizedPath, sitePath, type Locale } from '@/lib/site'
import { cn } from '@/lib/utils'

interface Props { locale: Locale }
const { locale } = Astro.props

const copy = {
  en: {
    eyebrow: 'Open source · macOS · Linux · Windows · Android · iOS',
    promise: 'One terminal pool. Every client can take control.',
    lead: 'AnyTTY is a terminal multiplexer built around a pool of persistent terminals. Terminals keep running on your machines, and the TUI, CLI, web, and mobile apps can view, control, and manage them from anywhere.',
    quickStart: 'Quick start', github: 'GitHub', release: 'Download beta',
    installLabel: 'macOS / Linux', windowsInstallLabel: 'Windows', installPlatformLabel: 'Choose an installer',
    installCommand: 'curl -fsSL https://raw.githubusercontent.com/anytty/anytty-site/main/install.sh | sh',
    windowsCommand: 'irm https://raw.githubusercontent.com/anytty/anytty-site/main/install.ps1 -OutFile install.ps1; ./install.ps1',
    copyCommand: 'Copy install command', copied: 'Copied',
    previewLabel: 'A real AnyTTY workspace', previewMeta: 'local + remote · 5 terminals · one workspace',
    previewAlt: 'AnyTTY TUI showing local and remote terminals in split and floating panels',
    tuiLabel: 'TUI interface', tabletLabel: 'Tablet view', mobileLabel: 'Mobile view',
    observerEyebrow: 'Close the window, keep the work', observerTitle: 'Close the window. The terminal keeps running.',
    observerLead: 'AnyTTY separates terminals from the windows that view them. Switch which terminal a panel shows without rebuilding your layout or restarting the task.',
    runtime: 'Terminal runtime', runtimeState: 'Keeps running on your machine', snapshots: 'Screen snapshots',
    views: ['TUI panel', 'CLI', 'WebView', 'Mobile'],
    poolEyebrow: 'Terminal pool', poolTitle: 'One daemon. Many terminals. Every client attached.',
    poolLead: 'AnyTTY keeps a pool of persistent terminals on your machine. TUI, CLI, web, and mobile clients attach to the same terminals without owning them.',
    observerPoints: [
      ['Switch terminals without touching your layout', 'The Terminal Picker changes what the current panel observes while your splits, floats, and workspace stay exactly where they are.'],
      ['A slow phone won\'t slow the task', 'Each device reads snapshots at its own pace, so a slow network or phone never makes the process inside the terminal wait.'],
    ],
    workbenchEyebrow: 'One workbench', workbenchTitle: 'Local and remote terminals in one place.',
    workbenchLead: 'Windows, macOS, and Linux are all first-class. Local, SSH, and Direct terminals show up in the same picker and behave like one workspace.',
    windowsAlt: 'AnyTTY TUI running on Windows with multiple terminals',
    workbenchPoints: [
      ['Native Windows support', 'CLI, TUI, and daemon builds for x64 and ARM64, with no WSL required for core workflows.'],
      ['Remote machines feel local', 'Operate a workstation, build machine, or remote AI agent the same way you operate a local terminal.'],
      ['Spot stalled work fast', 'See which terminal stopped producing output and find stuck tasks or agents without opening every session.'],
    ],
    historyEyebrow: 'History that outlives the screen', historyTitle: 'Long-running work shouldn\'t mean ever-growing memory.',
    historyLead: 'Output is written to files while the live view stays bounded. There is no fixed history line limit; practical capacity is limited by disk space, so history can keep growing as long as disk capacity does.',
    historyStats: [['Live memory', 'Bounded'], ['History', 'Disk-limited'], ['Reconnect', 'Full context']],
    historyNote: 'Useful after a disconnect, an accidental exit, or an unattended agent run that produced hours of output.',
    mobileEyebrow: 'Android + iOS', mobileTitle: 'See what\'s alive on your phone. Step in when it matters.',
    mobileLead: 'The mobile app is built for terminal work, not a desktop page squeezed onto a phone. Check recent activity, attach with a touch-optimized key row, and manage files over the same connection.',
    mobilePoints: [
      ['Terminal activity at a glance', 'Scan running sessions and time since last output.'],
      ['Touch-ready controls', 'Use Esc, Ctrl, arrows, paging, and common terminal actions.'],
      ['Files and previews', 'Browse, upload, download, and preview remote files.'],
    ],
    mobileAlts: ['AnyTTY mobile terminal list with recent activity', 'AnyTTY mobile terminal with an extra-key row', 'AnyTTY mobile remote file manager'],
    accessEyebrow: 'Reach your machines without loosening the boundary', accessTitle: 'Connect to what you own, keep the permissions yours.',
    accessLead: 'Use SSH when it already fits. When a machine sits behind NAT, the optional AnyTTY Cloud service can establish a peer-to-peer path or relay encrypted traffic while the local service keeps terminal and file permissions.',
    accessPoints: [
      ['Reach private-network machines', 'Connect through P2P hole punching when possible, with encrypted relay as the fallback.'],
      ['Encrypted by default', 'Remote terminal and file traffic is authenticated and encrypted instead of trusting the network path.'],
      ['Files travel with the session', 'Browse, upload, download, and manage files through the same protected connection.'],
    ],
    tabletEyebrow: 'Tablet workflows', tabletTitle: 'A full working surface when a phone isn\'t enough.',
    tabletLead: 'Run split-screen agent workflows or use terminal-native tools directly from the tablet client.',
    tabletCaptions: ['Codex and OpenCode in a vertical split', 'The TTT editor running inside a terminal'],
    tabletAlts: ['Tablet split screen running Codex and OpenCode', 'TTT editor running inside an AnyTTY terminal on a tablet'],
    compareEyebrow: 'Why AnyTTY', compareTitle: 'The terminal pool, compared.',
    compareLead: 'Close the window and see what survives. AnyTTY keeps a pool of persistent terminals your phone, tablet, and browser can all take over — tmux, Zellij, and Windows Terminal don\'t.',
    cloudEyebrow: 'AnyTTY Cloud', cloudTitle: 'Reach your machines, even behind NAT.',
    cloudLead: 'AnyTTY Cloud connects your machines through peer-to-peer paths with an encrypted relay fallback — no router config, no public IP required.',
    cloudCta: 'Open Cloud', cloudMeta: 'P2P hole punching · encrypted relay',
    compareProducts: ['AnyTTY', 'tmux', 'Zellij', 'Windows Terminal'],
    compareRows: [
      ['Persistent pool · survives closed windows', true, true, true, false],
      ['Unlimited history', true, false, false, false],
      ['Panels decoupled from terminals', true, false, false, false],
      ['Drive remote terminals like local ones', true, false, false, false],
      ['Full mouse support', true, false, false, false],
      ['Mobile & tablet clients', true, false, false, false],
      ['Preview remote files on mobile', true, false, false, false],
      ['Web browser access', true, false, false, false],
      ['Cross-platform (desktop + mobile)', true, false, false, false],
      ['Native Windows support (no WSL)', true, false, false, true],
      ['NAT traversal (P2P / relay)', true, false, false, false],
    ],
    installEyebrow: 'Beta 0.0.1', installTitle: 'Run it on the machine you already use.',
    installLead: 'The release includes binaries for macOS, Linux, and Windows, plus an unsigned Android beta APK. iOS is supported; public distribution is waiting for the App Store. Google Play and App Store submissions are in progress.',
    docs: 'Read the documentation', releases: 'Open GitHub Releases',
    platforms: ['macOS', 'Linux', 'Windows', 'Android', 'iOS'],
    openSource: 'Apache-2.0 · source, clients, and release artifacts on GitHub',
  },
  'zh-CN': {
    eyebrow: '开源 · macOS · Linux · Windows · Android · iOS',
    promise: '一个终端池，所有客户端都能接管。',
    lead: 'AnyTTY 是一个以终端池为核心的终端复用器。终端在你的机器上持续运行，TUI、CLI、Web 和手机 App 都可以随时查看、接管和管理它们。',
    quickStart: '快速开始', github: 'GitHub', release: '下载 Beta',
    installLabel: 'macOS / Linux', windowsInstallLabel: 'Windows', installPlatformLabel: '选择安装平台',
    installCommand: 'curl -fsSL https://raw.githubusercontent.com/anytty/anytty-site/main/install.sh | sh',
    windowsCommand: 'irm https://raw.githubusercontent.com/anytty/anytty-site/main/install.ps1 -OutFile install.ps1; ./install.ps1',
    copyCommand: '复制安装命令', copied: '已复制',
    previewLabel: '真实的 AnyTTY 工作台', previewMeta: '本地 + 远程 · 5 个终端 · 一个工作区',
    previewAlt: 'AnyTTY TUI 通过分屏和浮动 Panel 同时展示本地与远程终端',
    tuiLabel: 'TUI 界面', tabletLabel: '平板上的展示', mobileLabel: '手机上的展示',
    observerEyebrow: '关掉窗口，任务不中断', observerTitle: '关掉窗口，终端还在跑。',
    observerLead: 'AnyTTY 把终端和查看它的窗口分开。切换当前窗口观察的终端，不用重建布局，也不用重启任务。',
    runtime: '终端本体', runtimeState: '一直跑在你的机器上', snapshots: '画面快照',
    views: ['TUI 面板', 'CLI', 'WebView', '手机 App'],
    poolEyebrow: '终端池', poolTitle: '一个后台，多个终端，所有客户端随时接入。',
    poolLead: 'AnyTTY 在你的机器上维护一个持续运行的终端池。TUI、CLI、Web 和手机 App 都接入同一个终端池，但都不拥有终端的生命周期。',
    observerPoints: [
      ['想切哪个终端就切哪个', '切换当前窗口观察的终端时，分屏、浮动窗和工作区布局都保持不变。'],
      ['手机慢也不拖累任务', '每个设备按自己的速度读取画面。手机或网络慢一点，不会让终端里的程序停下来等。'],
    ],
    workbenchEyebrow: '一个工作台', workbenchTitle: '本地和远程终端，放在一起管理。',
    workbenchLead: 'Windows、macOS、Linux 都能用。本地终端、SSH 远程终端和直连终端都进同一个列表，操作方式一致。',
    windowsAlt: 'AnyTTY TUI 在 Windows 上管理多个终端',
    workbenchPoints: [
      ['Windows 原生支持', 'CLI、TUI 和后台服务都有 x64 与 ARM64 版本，核心功能不需要 WSL。'],
      ['远程机器像本地一样操作', '工作站、构建机或远程 Agent，都能像本地终端一样直接进入工作流。'],
      ['一眼看到谁卡住了', '按最近输出排序，快速找到长时间没有动静的终端或任务。'],
    ],
    historyEyebrow: '历史记录不丢', historyTitle: '跑很久，也不怕内存越占越多。',
    historyLead: '终端输出会写入文件，实时画面只保留有限数据。历史记录没有固定行数上限，主要受磁盘空间限制，跑多久都能继续记。',
    historyStats: [['实时内存', '有界'], ['历史容量', '看磁盘'], ['重新连接', '完整上下文']],
    historyNote: '断线、误关窗口，或者 Agent 跑了好几个小时，重新连上后还能看到完整现场。',
    mobileEyebrow: 'Android + iOS', mobileTitle: '手机上先看状态，再决定要不要接管。',
    mobileLead: '手机 App 不是把桌面页面硬塞进小屏幕。你可以快速查看哪些终端还在跑，用触控优化的按键操作，也能直接管理远程文件。',
    mobilePoints: [
      ['终端状态一眼看清', '查看运行中的终端和距离上次输出的时间。'],
      ['触控操作顺手', 'Esc、Ctrl、方向键、翻页和常用终端操作都做了触控适配。'],
      ['文件也能管', '浏览、上传、下载和预览远程文件。'],
    ],
    mobileAlts: ['AnyTTY 手机 App 展示终端列表与近期活动', 'AnyTTY 手机 App 使用扩展按键操作终端', 'AnyTTY 手机 App 的远程文件管理器'],
    accessEyebrow: '连接更简单，边界不放松', accessTitle: '能连上自己的机器，权限还是你的。',
    accessLead: '能用 SSH 就直接用 SSH。机器在内网时，AnyTTY Cloud 会尝试 P2P 直连，连不上就自动走加密中继；终端和文件权限仍然由本机后台服务控制。',
    accessPoints: [
      ['内网也能连', '优先 P2P 直连，连不上时自动使用加密中继。'],
      ['默认加密', '远程终端和文件流量都经过身份验证和加密。'],
      ['文件和终端走同一条连接', '浏览、上传、下载和管理文件，不用再单独开一条通道。'],
    ],
    tabletEyebrow: '平板工作流', tabletTitle: '手机不够用，就上平板。',
    tabletLead: '可以上下分屏同时跑两个 Agent，也可以在平板客户端里直接使用终端原生工具。',
    tabletCaptions: ['上下分屏同时运行 Codex 与 OpenCode', '在终端里运行 TTT 编辑器'],
    tabletAlts: ['平板上下分屏运行 Codex 与 OpenCode', '在平板 AnyTTY 终端内运行 TTT 编辑器'],
    compareEyebrow: '为什么是 AnyTTY', compareTitle: '终端池，一目了然。',
    compareLead: '关掉窗口，看看什么还在。AnyTTY 维护着一个持续运行的终端池，手机、平板和浏览器都能随时接管——tmux、Zellij 和 Windows Terminal 都没有这个池子。',
    cloudEyebrow: 'AnyTTY Cloud', cloudTitle: '内网机器，也能随时连上。',
    cloudLead: 'AnyTTY Cloud 通过 P2P 直连打通你的机器，连不上时自动走加密中继——不用改路由器，不需要公网 IP。',
    cloudCta: '进入 Cloud', cloudMeta: 'P2P 打洞 · 加密中继',
    compareProducts: ['AnyTTY', 'tmux', 'Zellij', 'Windows Terminal'],
    compareRows: [
      ['终端池 · 关窗不中断', true, true, true, false],
      ['无限历史记录', true, false, false, false],
      ['窗口与面板解绑', true, false, false, false],
      ['远程终端拉进本机 · 像本地窗口一样', true, false, false, false],
      ['全鼠标支持', true, false, false, false],
      ['手机 / 平板客户端', true, false, false, false],
      ['移动端远程文件预览', true, false, false, false],
      ['Web 浏览器访问', true, false, false, false],
      ['全平台（桌面 + 移动）', true, false, false, false],
      ['Windows 原生支持（无需 WSL）', true, false, false, true],
      ['内网穿透（P2P / 中继）', true, false, false, false],
    ],
    installEyebrow: 'Beta 0.0.1', installTitle: '直接装在你现在用的机器上。',
    installLead: '目前提供 macOS、Linux、Windows 安装包，以及未签名的 Android Beta APK。iOS 已支持，但公开安装要等 App Store；Google Play 和 App Store 都在推进中。',
    docs: '阅读文档', releases: '查看 GitHub Releases',
    platforms: ['macOS', 'Linux', 'Windows', 'Android', 'iOS'],
    openSource: 'Apache-2.0 · 源码、客户端和构建产物都在 GitHub',
  },
} as const

const t = copy[locale]
const docsPath = sitePath(localizedPath(locale, '/docs/quick-start'))
const releaseUrl = 'https://github.com/anytty/anytty-site/releases/tag/v0.0.1-beta.0'
const viewIcons = [PanelsTopLeft, TerminalSquare, Laptop, Smartphone]
const observerIcons = [Eye, ShieldCheck]
const workbenchIcons = [MonitorSmartphone, Laptop, Clock3]
const mobileIcons = [Clock3, TerminalSquare, FileStack]
const accessIcons = [Cloud, LockKeyhole, Files]
---

<main id="main" class="bg-background text-zinc-50">
  <div class="hero-cinematic -mt-20 pt-20">
    <section class="relative overflow-hidden px-5 pb-20 pt-24 text-center sm:px-8 sm:pt-32" aria-labelledby="product-title" data-hero>
      <div class="mx-auto max-w-4xl">
        <Badge variant="secondary" className="border-0 bg-[var(--chip)] px-3 py-1 font-mono text-[11px] font-normal text-zinc-200 shadow-none">
          {t.eyebrow}
        </Badge>
        <h1 id="product-title" class="mt-5 text-5xl font-medium leading-none tracking-tight sm:text-6xl">AnyTTY</h1>
        <p class="mt-5 text-2xl font-medium leading-snug text-zinc-100 sm:text-3xl">{t.promise}</p>
      </div>
      <div class="hero-set relative mx-auto mt-12 w-full max-w-6xl [perspective:2400px]">
        <div class="hero-scroll-image group origin-bottom overflow-hidden rounded-2xl bg-zinc-900 shadow-2xl ring-1 ring-[var(--line)]" data-hero-image>
          <img class="block aspect-[2862/1870] w-full cursor-zoom-in object-cover" src={sitePath('/assets/product/tui-main.webp')} alt={t.previewAlt} width="2862" height="1870" data-image-preview data-image-preview-center data-preview-label={t.tuiLabel} />
        </div>
        <figure class="hero-device hero-device-phone hidden lg:block" style={{ '--hero-tilt': '-8deg' }}>
          <img class="block w-full rounded-xl bg-zinc-900 object-cover ring-1 ring-[var(--line-strong)]" src={sitePath('/assets/product/mobile-terminals.webp')} alt={t.mobileAlts[0]} width="480" height="1068" loading="lazy" />
        </figure>
        <figure class="hero-device hero-device-pad hidden lg:block" style={{ '--hero-tilt': '6deg' }}>
          <img class="block w-full rounded-xl bg-zinc-900 object-cover ring-1 ring-[var(--line-strong)]" src={sitePath('/assets/product/pad-codex-opencode.webp')} alt={t.tabletAlts[0]} width="1200" height="1600" loading="lazy" />
        </figure>
      </div>
    </section>
  </div>

  <section class="py-20 sm:py-24 lg:py-28" aria-labelledby="pool-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="rounded-3xl bg-zinc-900/60 p-6 ring-1 ring-[var(--line)] sm:p-10 lg:p-14">
        <div class="grid gap-10 lg:grid-cols-[0.9fr_1.1fr] lg:items-center lg:gap-16">
          <div>
            <Badge variant="secondary" className="border-0 bg-[var(--chip)] font-mono text-[11px] font-normal text-zinc-200">{t.poolEyebrow}</Badge>
            <h2 id="pool-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.poolTitle}</h2>
            <p class="mt-5 max-w-2xl text-base font-normal leading-7 text-zinc-400">{t.poolLead}</p>
          </div>

          <div class="pool-waters rounded-2xl bg-zinc-950 p-4 ring-1 ring-[var(--line)] sm:p-6">
            <div class="grid gap-3 sm:grid-cols-2">
              {[
                ['build', 'running', 'make build && npm run test'],
                ['api', 'idle', 'go run ./cmd/anytty-cloud-controller'],
                ['agent', 'blocked', 'opencode --model deepseek-v4-flash'],
                ['files', 'running', 'anytty file watch ./logs'],
              ].map(([name, status, command], index) => (
                <div class="float-card rounded-xl bg-zinc-900 p-4 ring-1 ring-[var(--line)]" style={{ '--float-delay': `${index * 0.4}s` }}>
                  <div class="flex items-center justify-between gap-3">
                    <span class="font-mono text-[11px] font-normal text-zinc-300">{name}</span>
                    <span class={cn('flex items-center gap-1.5 font-mono text-[10px] font-normal', status === 'running' ? 'text-emerald-300' : status === 'blocked' ? 'text-amber-300' : 'text-zinc-400')}>
                      <span class={cn('size-1.5 rounded-full', status === 'running' ? 'bg-emerald-400' : status === 'blocked' ? 'bg-amber-400' : 'bg-zinc-500')} />
                      {status}
                    </span>
                  </div>
                  <p class="mt-3 overflow-hidden text-ellipsis whitespace-nowrap font-mono text-[11px] text-zinc-500">{command}</p>
                </div>
              ))}
            </div>
            <div class="pool-surface mt-4 rounded-xl bg-zinc-900 px-4 py-3 font-mono text-[11px] text-zinc-400 ring-1 ring-[var(--line)]">
              <span class="text-emerald-300">anytty terminal list</span> · 4 running · 1 idle · 0 detached
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="features" class="scroll-mt-20 py-20 sm:py-24 lg:py-28" aria-labelledby="observer-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="rounded-3xl bg-zinc-900/60 p-6 ring-1 ring-[var(--line)] sm:p-10 lg:p-14">
        <div class="grid gap-10 lg:grid-cols-2 lg:items-center lg:gap-16">
          <div>
            <Badge variant="secondary" className="border-0 bg-[var(--chip)] font-mono text-[11px] font-normal text-zinc-200">{t.observerEyebrow}</Badge>
            <h2 id="observer-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.observerTitle}</h2>
            <p class="mt-5 max-w-2xl text-base font-normal leading-7 text-zinc-400">{t.observerLead}</p>
            <div class="mt-8 grid gap-4">
              {t.observerPoints.map(([title, body], index) => {
                const Icon = observerIcons[index]
                return (
                  <div class="lift-card flex items-start gap-4 rounded-2xl bg-zinc-950 p-5 ring-1 ring-[var(--line)]">
                    <span class="grid size-10 shrink-0 place-items-center rounded-lg bg-emerald-400/10 text-emerald-300"><Icon className="size-5" aria-hidden="true" /></span>
                    <div>
                      <h3 class="text-sm font-medium">{title}</h3>
                      <p class="mt-1 text-sm font-normal leading-6 text-zinc-400">{body}</p>
                    </div>
                  </div>
                )
              })}
            </div>
          </div>

          <div class="rounded-2xl bg-zinc-950 p-6 ring-1 ring-[var(--line)]">
            <div class="flex items-center justify-between gap-4 font-mono text-[11px] font-normal text-zinc-400">
              <span class="flex items-center gap-2 text-zinc-200"><TerminalSquare className="size-4 animate-pulse text-emerald-400" aria-hidden="true" />{t.runtime}</span>
              <span>{t.runtimeState}</span>
            </div>
            <div class="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4">
              {t.views.map((view, index) => {
                const Icon = viewIcons[index]
                return (
                  <div class="lift-card flex min-h-24 flex-col items-center justify-center gap-2 rounded-xl bg-zinc-900 px-3 text-center ring-1 ring-[var(--line)]">
                    <Icon className="size-5 text-zinc-200" aria-hidden="true" />
                    <span class="font-mono text-[11px] font-normal text-zinc-400">{view}</span>
                  </div>
                )
              })}
            </div>
            <div class="mt-6 flex items-center gap-2 border-t border-[var(--line)] pt-4 font-mono text-[11px] font-normal text-zinc-400">
              <Radio className="size-3.5 text-sky-400" aria-hidden="true" />{t.snapshots}
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-20 sm:py-24 lg:py-28" aria-labelledby="workbench-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="rounded-3xl bg-zinc-900/60 p-6 ring-1 ring-[var(--line)] sm:p-10 lg:p-14">
        <div class="grid gap-10 lg:grid-cols-2 lg:items-center lg:gap-16">
          <div>
            <Badge variant="secondary" className="border-0 bg-[var(--chip)] font-mono text-[11px] font-normal text-zinc-200">{t.workbenchEyebrow}</Badge>
            <h2 id="workbench-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.workbenchTitle}</h2>
            <p class="mt-5 max-w-2xl text-base font-normal leading-7 text-zinc-400">{t.workbenchLead}</p>
            <div class="mt-8 grid gap-4">
              {t.workbenchPoints.map(([title, body], index) => {
                const Icon = workbenchIcons[index]
                const iconColor = index === 0 ? 'bg-sky-400/10 text-sky-300' : index === 1 ? 'bg-emerald-400/10 text-emerald-300' : 'bg-amber-400/10 text-amber-300'
                return (
                  <div class="lift-card flex items-start gap-4 rounded-2xl bg-zinc-950 p-5 ring-1 ring-[var(--line)]">
                    <span class={cn('grid size-10 shrink-0 place-items-center rounded-lg', iconColor)}><Icon className="size-5" aria-hidden="true" /></span>
                    <div>
                      <h3 class="text-sm font-medium">{title}</h3>
                      <p class="mt-1 text-sm font-normal leading-6 text-zinc-400">{body}</p>
                    </div>
                  </div>
                )
              })}
            </div>
          </div>
          <figure class="float-card group overflow-hidden rounded-2xl bg-zinc-950 ring-1 ring-[var(--line)]">
            <img class="block aspect-[1800/955] w-full cursor-zoom-in object-cover" src={sitePath('/assets/product/win-tui.webp')} alt={t.windowsAlt} width="1800" height="955" loading="lazy" data-image-preview />
          </figure>
        </div>
      </div>
    </div>
  </section>

  <section class="py-20 sm:py-24 lg:py-28" aria-labelledby="history-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="rounded-3xl bg-zinc-900/60 p-6 ring-1 ring-[var(--line)] sm:p-10 lg:p-14">
        <div class="grid gap-10 lg:grid-cols-[1.1fr_0.9fr] lg:items-end lg:gap-16">
          <div>
            <Badge variant="secondary" className="border-0 bg-[var(--chip)] font-mono text-[11px] font-normal text-zinc-200">{t.historyEyebrow}</Badge>
            <h2 id="history-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.historyTitle}</h2>
            <p class="mt-5 max-w-2xl text-base font-normal leading-7 text-zinc-400">{t.historyLead}</p>
            <div class="lift-card mt-7 flex max-w-2xl items-start gap-3 rounded-2xl bg-zinc-950 p-5 text-sm font-normal leading-6 text-zinc-300 ring-1 ring-[var(--line)]">
              <History className="mt-0.5 size-5 shrink-0 text-sky-400" aria-hidden="true" />{t.historyNote}
            </div>
          </div>
          <dl class="grid gap-3 sm:grid-cols-3 lg:grid-cols-1">
            {t.historyStats.map(([term, detail]) => (
              <div class="lift-card flex min-h-20 items-center justify-between gap-6 rounded-2xl bg-zinc-950 px-5 py-4 ring-1 ring-[var(--line)]">
                <dt class="font-mono text-[11px] font-normal text-zinc-400">{term}</dt><dd class="text-lg font-medium">{detail}</dd>
              </div>
            ))}
          </dl>
        </div>
      </div>
    </div>
  </section>

  <section id="screens" class="scroll-mt-20 py-20 sm:py-24 lg:py-28" aria-labelledby="mobile-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="rounded-3xl bg-zinc-900/60 p-6 ring-1 ring-[var(--line)] sm:p-10 lg:p-14">
        <div class="grid gap-10 lg:grid-cols-2 lg:items-center lg:gap-16">
          <div>
            <Badge variant="secondary" className="border-0 bg-[var(--chip)] font-mono text-[11px] font-normal text-zinc-200">{t.mobileEyebrow}</Badge>
            <h2 id="mobile-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.mobileTitle}</h2>
            <p class="mt-5 max-w-2xl text-base font-normal leading-7 text-zinc-400">{t.mobileLead}</p>
            <div class="mt-8 grid gap-4">
              {t.mobilePoints.map(([title, body], index) => {
                const Icon = mobileIcons[index]
                return (
                  <div class="lift-card flex items-start gap-4 rounded-2xl bg-zinc-950 p-5 ring-1 ring-[var(--line)]">
                    <span class="grid size-10 shrink-0 place-items-center rounded-lg bg-emerald-400/10 text-emerald-300"><Icon className="size-5" aria-hidden="true" /></span>
                    <div>
                      <h3 class="text-sm font-medium">{title}</h3>
                      <p class="mt-1 text-sm font-normal leading-6 text-zinc-400">{body}</p>
                    </div>
                  </div>
                )
              })}
            </div>
          </div>
          <div class="grid grid-cols-1 gap-5 sm:grid-cols-3">
            {['mobile-terminals.webp', 'mobile-terminal.webp', 'mobile-files.webp'].map((image, index) => (
              <figure class="float-card group overflow-hidden rounded-2xl bg-zinc-950 ring-1 ring-[var(--line)]" style={{ '--float-delay': `${index * 0.5}s` }}>
                <img class="block aspect-[480/1068] w-full object-cover" src={sitePath(`/assets/product/${image}`)} alt={t.mobileAlts[index]} width="480" height="1068" loading="lazy" />
              </figure>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-20 sm:py-24 lg:py-28" aria-labelledby="access-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="rounded-3xl bg-zinc-900/60 p-6 ring-1 ring-[var(--line)] sm:p-10 lg:p-14">
        <div class="mx-auto max-w-3xl text-center">
          <Badge variant="secondary" className="border-0 bg-[var(--chip)] font-mono text-[11px] font-normal text-zinc-200">{t.accessEyebrow}</Badge>
          <h2 id="access-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.accessTitle}</h2>
          <p class="mt-5 text-base font-normal leading-7 text-zinc-400">{t.accessLead}</p>
        </div>
        <div class="mt-10 grid gap-4 md:grid-cols-3">
          {t.accessPoints.map(([title, body], index) => {
            const Icon = accessIcons[index]
            const tone = index === 0 ? 'bg-sky-400/10 text-sky-300' : index === 1 ? 'bg-emerald-400/10 text-emerald-300' : 'bg-amber-400/10 text-amber-300'
            return (
              <div class="lift-card rounded-2xl bg-zinc-950 p-6 ring-1 ring-[var(--line)]">
                <span class={cn('grid size-11 place-items-center rounded-lg', tone)}><Icon aria-hidden="true" /></span>
                <h3 class="mt-5 text-base font-medium">{title}</h3>
                <p class="mt-2 text-sm font-normal leading-6 text-zinc-400">{body}</p>
              </div>
            )
          })}
        </div>
      </div>
    </div>
  </section>

  <section class="py-20 sm:py-24 lg:py-28" aria-labelledby="tablet-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="rounded-3xl bg-zinc-900/60 p-6 ring-1 ring-[var(--line)] sm:p-10 lg:p-14">
        <div class="max-w-3xl">
          <Badge variant="secondary" className="border-0 bg-[var(--chip)] font-mono text-[11px] font-normal text-zinc-200">{t.tabletEyebrow}</Badge>
          <h2 id="tablet-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.tabletTitle}</h2>
          <p class="mt-5 max-w-2xl text-base font-normal leading-7 text-zinc-400">{t.tabletLead}</p>
        </div>
        <div class="mt-10 grid gap-6 md:grid-cols-2">
          {['pad-codex-opencode.webp', 'pad-ttt-editor.webp'].map((image, index) => (
            <figure class="group">
              <div class="float-card overflow-hidden rounded-2xl" style={{ '--float-delay': `${index * 0.6}s` }}>
                <img class="block w-full object-cover ring-1 ring-[var(--line)]" src={sitePath(`/assets/product/${image}`)} alt={t.tabletAlts[index]} width="1200" height={index === 0 ? '1600' : '1528'} loading="lazy" />
              </div>
              <figcaption class="mt-3 text-sm font-normal text-zinc-500">{t.tabletCaptions[index]}</figcaption>
            </figure>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section id="compare" class="scroll-mt-20 py-20 sm:py-24 lg:py-28" aria-labelledby="compare-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="rounded-3xl bg-zinc-900/60 p-6 ring-1 ring-[var(--line)] sm:p-10 lg:p-14">
        <div class="max-w-3xl">
          <Badge variant="secondary" className="border-0 bg-[var(--chip)] font-mono text-[11px] font-normal text-zinc-200">{t.compareEyebrow}</Badge>
          <h2 id="compare-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.compareTitle}</h2>
          <p class="mt-5 max-w-2xl text-base font-normal leading-7 text-zinc-400">{t.compareLead}</p>
        </div>
        <div class="mt-10 overflow-x-auto">
          <table class="w-full min-w-[720px] border-separate border-spacing-0 text-left">
            <thead>
              <tr>
                <th class="pb-4 pr-4 text-left font-mono text-[11px] font-normal text-zinc-500">{t.compareEyebrow}</th>
                {t.compareProducts.map((product, index) => (
                  <th class={cn('px-4 pb-4 text-center', index === 0 ? 'text-base font-medium text-sky-300' : 'text-sm font-normal text-zinc-500')}>{product}</th>
                ))}
              </tr>
            </thead>
            <tbody>
              {t.compareRows.map(([label, ...values]) => (
                <tr class="transition-colors hover:bg-[var(--chip-soft)]">
                  <td class="border-t border-[var(--line-soft)] py-3.5 pr-4 text-sm text-zinc-300">{label}</td>
                  {values.map((ok, index) => (
                    <td class={cn('border-t border-[var(--line-soft)] py-3.5 text-center', index === 0 && 'bg-sky-400/[0.06]')}>
                      {ok
                        ? <Check className={cn('mx-auto size-4', index === 0 ? 'text-emerald-400' : 'text-zinc-500')} aria-hidden="true" />
                        : <Minus className="mx-auto size-4 text-zinc-600" aria-hidden="true" />}
                    </td>
                  ))}
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </section>

  <section class="py-20 sm:py-24 lg:py-28" aria-labelledby="cloud-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="relative overflow-hidden rounded-3xl bg-zinc-900/60 p-8 ring-1 ring-[var(--line)] sm:p-12 lg:p-16">
        <div class="pointer-events-none absolute inset-0 opacity-80" style="background: radial-gradient(48% 65% at 50% 0%, rgba(56, 189, 248, 0.12), transparent 70%);"></div>
        <div class="relative mx-auto max-w-2xl text-center">
          <Badge variant="secondary" className="border-0 bg-[var(--chip)] font-mono text-[11px] font-normal text-zinc-200">{t.cloudEyebrow}</Badge>
          <h2 id="cloud-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.cloudTitle}</h2>
          <p class="mt-5 text-base font-normal leading-7 text-zinc-400">{t.cloudLead}</p>
          <a class={cn(buttonVariants({ variant: 'default', size: 'lg' }), 'mt-8 h-11 gap-2 bg-sky-500 px-6 font-normal hover:bg-sky-400')} href="https://cloud.anytty.com">
            <Cloud className="size-4" aria-hidden="true" />{t.cloudCta}<ArrowRight aria-hidden="true" />
          </a>
          <p class="mt-5 font-mono text-[11px] font-normal text-zinc-500">{t.cloudMeta}</p>
        </div>
      </div>
    </div>
  </section>

  <section id="install" class="scroll-mt-20 py-20 sm:py-24 lg:py-28" aria-labelledby="install-title">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="rounded-3xl bg-zinc-900/60 p-6 ring-1 ring-[var(--line)] sm:p-10 lg:p-14">
        <div class="grid gap-10 lg:grid-cols-[0.8fr_1.2fr] lg:gap-20">
          <div class="min-w-0">
            <Badge variant="secondary" className="border-0 bg-[var(--chip)] font-mono text-[11px] font-normal text-zinc-200">{t.installEyebrow}</Badge>
            <h2 id="install-title" class="mt-5 text-3xl font-medium leading-tight sm:text-4xl">{t.installTitle}</h2>
            <p class="mt-5 text-base font-normal leading-7 text-zinc-400">{t.installLead}</p>
            <div class="mt-7 flex flex-wrap gap-2" aria-label={t.platforms.join(', ')}>
              {t.platforms.map((platform) => <Badge variant="outline" className="border-[var(--line-strong)] bg-[var(--chip-soft)] font-mono text-[11px] font-normal text-zinc-300">{platform}</Badge>)}
            </div>
          </div>
          <div class="min-w-0">
            <InstallTabs
              client:load
              ariaLabel={t.installPlatformLabel}
              unixLabel={t.installLabel}
              windowsLabel={t.windowsInstallLabel}
              unixCommand={t.installCommand}
              windowsCommand={t.windowsCommand}
              copyLabel={t.copyCommand}
              copiedLabel={t.copied}
            />
            <p class="mt-4 flex items-start gap-2 text-sm font-normal leading-6 text-zinc-400"><CheckCircle2 className="mt-0.5 size-4 shrink-0 text-emerald-400" aria-hidden="true" />{t.openSource}</p>
            <div class="mt-8 flex flex-wrap gap-3">
              <a class={cn(buttonVariants({ variant: 'default', size: 'lg' }), 'h-11 px-5 font-normal')} href={docsPath}>{t.docs}<ArrowRight aria-hidden="true" /></a>
              <a class={cn(buttonVariants({ variant: 'outline', size: 'lg' }), 'h-11 border-[var(--line-strong)] bg-[var(--chip-soft)] px-5 font-normal text-zinc-100 hover:bg-[var(--chip)] hover:text-zinc-100')} href={releaseUrl}>{t.releases}<ArrowDownToLine aria-hidden="true" /></a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</main>

<script>
  const hero = document.querySelector('[data-hero]')
  const image = hero?.querySelector<HTMLElement>('[data-hero-image]')
  if (hero && image) {
    let hoverScale = 1
    const apply = (withTransition: boolean) => {
      image.style.transition = withTransition ? 'transform 400ms ease' : 'none'
      const rect = hero.getBoundingClientRect()
      const progress = Math.min(Math.max(-rect.top / window.innerHeight, 0), 1)
      image.style.transform = `scale(${(1.04 - progress * 0.04) * hoverScale}) rotateX(${8 - progress * 8}deg) translateY(${progress * 2}%)`
    }
    addEventListener('scroll', () => apply(false), { passive: true })
    addEventListener('resize', () => apply(false))
    image.addEventListener('mouseenter', () => {
      hoverScale = 1.03
      apply(true)
    })
    image.addEventListener('mouseleave', () => {
      hoverScale = 1
      apply(true)
    })
    apply(false)
  }

  const previews = Array.from(document.querySelectorAll<HTMLImageElement>('[data-image-preview]'))
  const heroSet = document.querySelector<HTMLElement>('.hero-set')
  let active: HTMLDivElement | null = null
  let activeImage: HTMLImageElement | null = null
  let closingNode: HTMLDivElement | null = null
  let closingImage: HTMLImageElement | null = null
  let labelEl: HTMLDivElement | null = null

  const updateHeroLayer = () => {
    const previewing = activeImage !== null && activeImage.dataset.imagePreviewCenter !== undefined
    heroSet?.classList.toggle('hero-previewing', previewing)
  }

  const removeActive = (immediate = false) => {
    if (!active) return
    if (immediate) {
      active.remove()
      if (activeImage) activeImage.style.opacity = ''
      labelEl?.remove()
      labelEl = null
      active = null
      activeImage = null
      updateHeroLayer()
      return
    }
    closingNode = active
    closingImage = activeImage
    if (activeImage) activeImage.style.opacity = ''
    labelEl?.remove()
    labelEl = null
    active = null
    activeImage = null
    updateHeroLayer()
    closingNode.classList.add('image-preview-inline-out')
    closingNode.addEventListener('animationend', () => {
      closingNode?.remove()
      closingNode = null
      closingImage = null
    }, { once: true })
  }

  const cancelClose = () => {
    if (!closingNode) return
    closingNode.classList.remove('image-preview-inline-out')
    active = closingNode
    activeImage = closingImage
    if (closingImage) closingImage.style.opacity = '0'
    closingNode = null
    closingImage = null
    updateHeroLayer()
  }

  const isWithin = (x: number, y: number, rect: DOMRect) =>
    x >= rect.left && x <= rect.right && y >= rect.top && y <= rect.bottom

  const clamp = (value: number, min: number, max: number) => Math.min(Math.max(value, min), max)

  const previewScaleFor = (width: number) =>
    width >= 900 ? 1.08 : width >= 700 ? 1.5 : width >= 500 ? 1.2 : 1.75

  const positionPreview = (node: HTMLDivElement, image: HTMLImageElement, scale: number, centered: boolean, overlayW: number, overlayH: number) => {
    const rect = image.getBoundingClientRect()
    const vw = window.innerWidth
    const vh = window.innerHeight

    const scaledW = overlayW * scale
    const scaledH = overlayH * scale
    const centerX = rect.left + overlayW / 2
    const centerY = rect.top + overlayH / 2

    const naturalLeft = centerX - scaledW / 2
    const naturalTop = centerY - scaledH / 2

    const targetLeft = centered
      ? (vw - scaledW) / 2
      : scaledW > vw - 32
        ? (vw - scaledW) / 2
        : clamp(naturalLeft, 16, vw - scaledW - 16)
    const targetTop = centered
      ? (vh - scaledH) / 2
      : scaledH > vh - 32
        ? (vh - scaledH) / 2
        : clamp(naturalTop, 16, vh - scaledH - 16)

    node.style.left = `${rect.left}px`
    node.style.top = `${rect.top}px`
    node.style.width = `${overlayW}px`
    node.style.height = `${overlayH}px`
    node.style.setProperty('--preview-tx', `${targetLeft - naturalLeft}px`)
    node.style.setProperty('--preview-ty', `${targetTop - naturalTop}px`)
  }

  const clearClosing = () => {
    if (closingNode) {
      closingNode.remove()
      closingNode = null
      closingImage = null
    }
  }

  const showPreview = (image: HTMLImageElement) => {
    clearClosing()
    const rect = image.getBoundingClientRect()
    if (rect.width === 0 || rect.height === 0) return

    const centered = image.dataset.imagePreviewCenter !== undefined
    const vw = window.innerWidth
    const vh = window.innerHeight

    let overlayW = rect.width
    let overlayH = rect.height
    if (centered && image.naturalWidth > 0) {
      overlayH = rect.width * image.naturalHeight / image.naturalWidth
    }

    let scale = previewScaleFor(rect.width)
    if (centered) {
      scale = Math.max(1.05, Math.min(scale, (vw - 32) / overlayW, (vh - 32) / overlayH))
    }
    activeScale = scale

    const node = document.createElement('div')
    node.className = 'image-preview-inline'
    node.style.setProperty('--preview-scale', scale.toString())
    positionPreview(node, image, scale, centered, overlayW, overlayH)

    const copy = document.createElement('img')
    copy.src = image.currentSrc || image.src
    copy.alt = image.alt || ''
    node.appendChild(copy)

    const label = image.dataset.previewLabel
    if (label) {
      const caption = document.createElement('div')
      caption.className = 'image-preview-label'
      caption.textContent = label
      document.body.appendChild(caption)
      labelEl = caption
    }

    document.body.appendChild(node)
    image.style.opacity = '0'

    active = node
    activeImage = image
    updateHeroLayer()
    positionLabel()
  }

  const positionLabel = () => {
    if (!labelEl || !active || !activeImage) return
    const rect = activeImage.getBoundingClientRect()
    const tx = parseFloat(active.style.getPropertyValue('--preview-tx')) || 0
    const ty = parseFloat(active.style.getPropertyValue('--preview-ty')) || 0
    const overlayW = active.offsetWidth
    const overlayH = active.offsetHeight
    const scaledW = overlayW * activeScale
    const scaledH = overlayH * activeScale
    const centerX = rect.left + overlayW / 2 + tx
    const centerY = rect.top + overlayH / 2 + ty
    const top = centerY + scaledH / 2
    const vh = window.innerHeight
    const y = top + 16 + 36 > vh ? centerY - scaledH / 2 - 46 : top + 16
    labelEl.style.left = `${centerX}px`
    labelEl.style.top = `${y}px`
  }

  let lastX = 0
  let lastY = 0
  let activeScale = 0
  let lastSwitchAt = 0

  const syncActive = () => {
    if (!active || !activeImage) return
    const centered = activeImage.dataset.imagePreviewCenter !== undefined
    const rect = activeImage.getBoundingClientRect()
    let overlayW = rect.width
    let overlayH = rect.height
    if (centered && activeImage.naturalWidth > 0) {
      overlayH = rect.width * activeImage.naturalHeight / activeImage.naturalWidth
    }
    positionPreview(active, activeImage, activeScale, centered, overlayW, overlayH)
    positionLabel()
  }

  const isActiveUnder = (x: number, y: number) => {
    if (!active) return false
    return isWithin(x, y, active.getBoundingClientRect()) ||
      (activeImage !== null && isWithin(x, y, activeImage.getBoundingClientRect()))
  }

  addEventListener('pointermove', (event) => {
    const { clientX: x, clientY: y } = event
    lastX = x
    lastY = y

    if (closingNode && closingImage && isWithin(x, y, closingImage.getBoundingClientRect())) {
      cancelClose()
      return
    }

    const top = document.elementFromPoint(x, y)
    const onActiveLayer = active !== null && top !== null && (top === active || active.contains(top))
    if (onActiveLayer) return

    const other = previews.find((image) =>
      image !== activeImage &&
      isWithin(x, y, image.getBoundingClientRect()) &&
      !onActiveLayer,
    )
    if (other) {
      const now = Date.now()
      if (now - lastSwitchAt > 220) {
        if (active && activeImage !== other) removeActive(true)
        if (!active) showPreview(other)
        lastSwitchAt = now
      }
      return
    }

    if (isActiveUnder(x, y)) return

    const next = previews.find((image) => isWithin(x, y, image.getBoundingClientRect()))
    if (next) {
      const now = Date.now()
      if (now - lastSwitchAt > 220) {
        if (active && activeImage !== next) removeActive(true)
        if (!active) showPreview(next)
        lastSwitchAt = now
      }
    } else {
      removeActive()
    }
  }, { passive: true })

  const isRectInViewport = (rect: DOMRect) =>
    rect.bottom > 0 && rect.top < window.innerHeight && rect.right > 0 && rect.left < window.innerWidth

  addEventListener('scroll', () => {
    if (!active && !closingNode) return
    if (!activeImage) return
    const rect = activeImage.getBoundingClientRect()
    if (!isRectInViewport(rect) || !isWithin(lastX, lastY, rect)) {
      removeActive()
      return
    }
    syncActive()
  }, { passive: true })
  addEventListener('resize', () => {
    if (!active && !closingNode) return
    syncActive()
    if (!isActiveUnder(lastX, lastY)) removeActive()
  })
  addEventListener('blur', () => {
    labelEl?.remove()
    labelEl = null
    if (closingNode) {
      closingNode.remove()
      closingNode = null
      closingImage = null
    }
    removeActive()
  })

  document.querySelectorAll<HTMLElement>('.float-card, .lift-card').forEach((card) => {
    card.addEventListener('pointermove', (event) => {
      const rect = card.getBoundingClientRect()
      card.style.setProperty('--ripple-x', `${event.clientX - rect.left}px`)
      card.style.setProperty('--ripple-y', `${event.clientY - rect.top}px`)
    })
  })
</script>

<style>
  .hero-set .hero-scroll-image,
  .hero-set .hero-device {
    transition: box-shadow 300ms ease;
  }

  .hero-set:has(.hero-scroll-image:hover) .hero-device,
  .hero-set:has(.hero-device:hover) .hero-scroll-image {
    box-shadow: 0 24px 64px rgba(56, 189, 248, 0.2);
  }

  .hero-set.hero-previewing .hero-device {
    z-index: 45;
  }

  .hero-device {
    position: absolute;
    z-index: 10;
    overflow: hidden;
    border-radius: 1rem;
    box-shadow: 0 24px 48px rgba(0, 0, 0, 0.5);
    animation: hero-float 4.4s ease-in-out infinite;
    animation-delay: var(--float-delay, 0s);
  }

  .hero-device:hover {
    animation-play-state: paused;
    --hero-scale: 1.06;
    box-shadow: 0 24px 64px rgba(56, 189, 248, 0.25);
  }

  .hero-device-phone {
    left: -2.25rem;
    top: 32%;
    width: 9.5rem;
  }

  .hero-device-pad {
    right: -1.5rem;
    top: 12%;
    width: 16rem;
  }

  @keyframes hero-float {
    0%,
    100% {
      transform: rotateX(8deg) rotate(var(--hero-tilt, 0deg)) translateY(0) scale(var(--hero-scale, 1));
    }

    50% {
      transform: rotateX(8deg) rotate(var(--hero-tilt, 0deg)) translateY(-8px) scale(var(--hero-scale, 1));
    }
  }

  @media (min-width: 1280px) {
    .hero-device-phone {
      left: -3.75rem;
      width: 10.5rem;
    }

    .hero-device-pad {
      right: -3.5rem;
      width: 17rem;
    }
  }

  @media (min-width: 1536px) {
    .hero-device-phone {
      left: -5.75rem;
      width: 11rem;
    }

    .hero-device-pad {
      right: -6rem;
      width: 18rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-device {
      animation: none;
    }

    .hero-device:hover {
      transform: rotateX(8deg) rotate(var(--hero-tilt, 0deg)) scale(1.06);
    }
  }

  .pool-waters {
    background-image:
      radial-gradient(70% 45% at 50% 0%, rgba(37, 99, 235, 0.12), transparent 70%),
      radial-gradient(45% 35% at 88% 100%, rgba(14, 165, 233, 0.08), transparent 65%);
  }

  .float-card {
    position: relative;
    overflow: hidden;
    animation: pool-bob 3.6s ease-in-out infinite;
    animation-delay: var(--float-delay, 0s);
    transition: box-shadow 300ms ease;
  }

  .float-card:hover {
    animation-play-state: paused;
    box-shadow: 0 8px 32px rgba(56, 189, 248, 0.16);
  }

  .lift-card {
    position: relative;
    overflow: hidden;
    transition: box-shadow 300ms ease, transform 300ms ease;
  }

  .lift-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(56, 189, 248, 0.16);
  }

  .float-card::before,
  .lift-card::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    pointer-events: none;
    background: radial-gradient(120px circle at var(--ripple-x, 50%) var(--ripple-y, 50%), rgba(56, 189, 248, 0.18), transparent 70%);
    opacity: 0;
    transition: opacity 250ms ease;
  }

  .float-card:hover::before,
  .lift-card:hover::before {
    opacity: 1;
  }

  .float-card::after,
  .lift-card::after {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    pointer-events: none;
    background: radial-gradient(circle at var(--ripple-x, 50%) var(--ripple-y, 50%), rgba(125, 211, 252, 0.3), transparent 45%);
    opacity: 0;
    transform-origin: var(--ripple-x, 50%) var(--ripple-y, 50%);
  }

  .float-card:hover::after,
  .lift-card:hover::after {
    animation: pool-ripple 700ms ease-out;
  }

  .pool-surface {
    position: relative;
    overflow: hidden;
  }

  .pool-surface::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(56, 189, 248, 0.5), transparent);
  }

  @keyframes pool-bob {
    0%,
    100% {
      transform: translateY(0);
    }

    50% {
      transform: translateY(-4px);
    }
  }

  @keyframes pool-ripple {
    0% {
      opacity: 0;
      transform: scale(0.3);
    }

    25% {
      opacity: 1;
    }

    100% {
      opacity: 0;
      transform: scale(1.7);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .float-card {
      animation: none;
    }

    .float-card:hover::after,
    .lift-card:hover::after {
      animation: none;
    }
  }
</style>
